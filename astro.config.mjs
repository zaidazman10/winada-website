import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // REPLACE 'YOUR_GITHUB_USERNAME' with your actual GitHub username (e.g., 'matzaid')
  site: 'https://YOUR_GITHUB_USERNAME.github.io',
  
  // REPLACE 'winada-website' with your actual repository name on GitHub
  base: '/winada-website',
});
